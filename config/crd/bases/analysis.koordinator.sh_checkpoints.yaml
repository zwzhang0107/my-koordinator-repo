---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: checkpoints.analysis.koordinator.sh
spec:
  group: analysis.koordinator.sh
  names:
    kind: Checkpoint
    listKind: CheckpointList
    plural: checkpoints
    shortNames:
    - cp
    singular: checkpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .spec.modelname
      name: Model
      type: string
    - jsonPath: .spec.workloadnamspace
      name: namespace
      type: string
    - jsonPath: .spec.workloadname
      name: Name
      type: string
    - jsonPath: .status.lastupdateTime
      name: LastUpdateTime
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Checkpoint is the model data that is used for recovery after
          restart.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: 'Specification of the checkpoint. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#spec-and-status.'
            properties:
              modelArgs:
                description: Arguments of the model using
                type: object
                x-kubernetes-preserve-unknown-fields: true
              modelName:
                description: Name of Model that this checkpoint using
                type: string
              workloadName:
                description: Name of the workload that this checkpoint belongs to
                type: string
              workloadNamespace:
                description: Namespace of the workload that this checkpoint belongs
                  to
                type: string
            type: object
          status:
            description: Data of the checkpoint.
            properties:
              firstSampleStart:
                description: Timestamp of the fist sample from the model.
                format: date-time
                nullable: true
                type: string
              lastSampleStart:
                description: Timestamp of the last sample from the model.
                format: date-time
                nullable: true
                type: string
              lastUpdateTime:
                description: The time when the status was last refreshed.
                format: date-time
                nullable: true
                type: string
              modelData:
                description: data of the model
                type: object
                x-kubernetes-preserve-unknown-fields: true
              totalSamplesCount:
                description: Total number of samples in the model.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
